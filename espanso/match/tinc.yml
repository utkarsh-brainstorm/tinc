# ============================================================
#  Tinc Master Match File  |  tinc.yml
#  Project: https://github.com/utkarsh-brainstorm/tinc
# ============================================================
#
#  SYNTAX:  [your text here]command
#  All async triggers instantly place "Bas ittu sa time aur..."
#
#  ── AI CORE ─────────────────────────────────────────────────
#    [prompt]ai    Direct answer, zero system prompt
#    [prompt]av    ai + clipboard as context  (i→v)
#    [prompt]ad    Short, precise answer (1-2 lines max)
#
#  ── WRITING ─────────────────────────────────────────────────
#    [text]fix     Fix spelling/grammar, preserve tone exactly
#    [text]tldr    1-2 sentence TL;DR
#
#  ── CODE ────────────────────────────────────────────────────
#    [prompt]ref   Refactor + comment
#    [prompt]py    Python only
#    [prompt]cp    C++ only
#    [prompt]sh    Bash only
#    [prompt]htm   HTML only
#    [prompt]csv   CSV only
#    [prompt]json  JSON only
#
#  ── CLIPBOARD VARIANTS (replace last char with 'v') ─────────
#    []fiv    Fix clipboard text
#    []tldv   TL;DR clipboard text
#    []rev    Refactor clipboard code
#    []pv     Clipboard → Python
#    []cv     Clipboard → C++
#    []sv     Clipboard → Bash
#    []htv    Clipboard → HTML
#    []jsov   Clipboard → JSON
#    []csj    Clipboard → CSV
#    []av     Clipboard as context for ai answer
#
#  ── TRANSLATION ─────────────────────────────────────────────
#    [text]hi    Hindi translation
#    [text]hd    Romanized → Devanagari
#    [text]hu    Hinglish → Hindi
#
#  ── MISC ────────────────────────────────────────────────────
#    :today  :time  :myip
#
# ============================================================

matches:
  # ─────────────────────────────────────────────────────────
  # MISC
  # ─────────────────────────────────────────────────────────

  - trigger: ":today"
    replace: "{{d}}"
    vars:
      - name: d
        type: date
        params:
          format: "%Y-%m-%d"

  - trigger: ":time"
    replace: "{{t}}"
    vars:
      - name: t
        type: date
        params:
          format: "%H:%M:%S"

  - trigger: ":myip"
    replace: "{{ip}}"
    vars:
      - name: ip
        type: shell
        params:
          cmd: "curl -s ifconfig.me"

  # ═══════════════════════════════════════════════════════════
  #  TRANSLATION
  # ═══════════════════════════════════════════════════════════

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?hi)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?hd)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?hu)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # ═══════════════════════════════════════════════════════════
  #  AI CORE
  # ═══════════════════════════════════════════════════════════

  # ai — direct answer, no system prompt
  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?ai)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # av — ai + clipboard context  (i→v)
  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?av)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # ad — short, precise answer (1-2 lines)
  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?ad)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # ═══════════════════════════════════════════════════════════
  #  WRITING
  # ═══════════════════════════════════════════════════════════

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?fix)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?tldr)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # ═══════════════════════════════════════════════════════════
  #  CODE OUTPUT
  # ═══════════════════════════════════════════════════════════

  - regex: "\\[(?P<prompt>[^\\]]+)\\](?P<suffix>w?ref)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?py)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?cp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?sh)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?htm)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?csv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?json)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  # ═══════════════════════════════════════════════════════════
  #  CLIPBOARD VARIANTS — replace last char of suffix with 'v'
  # ═══════════════════════════════════════════════════════════

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?fiv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?tldv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?rev)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?pv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?cv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?sv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?htv)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?jsov)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?csj)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"
  # ═══════════════════════════════════════════════════════════
  #  PTYXIS / TERMINAL VARIANTS & CMD
  # ═══════════════════════════════════════════════════════════

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?aip)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?adp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?fixp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?tldrp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?refp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?pyp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?cpp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?shp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?cmd)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

  - regex: "\\[(?P<prompt>[^\\]]*)\\](?P<suffix>w?cmdp)$"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "ESPANSO_MODE={{suffix}} ESPANSO_TEXT='{{prompt}}' python3 ~/.config/espanso/scripts/ai_router.py"

